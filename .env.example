# Copy to .env for local dev. On Netlify: Site configuration → Environment variables (same names, then redeploy).
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_public_key
